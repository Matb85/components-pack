import mixin, { type StoreDataI } from "./mixin";
import { Store, mutations } from "./store";
import photo from "./photo";
import maputil from "./map";

export { mixin, Store, mutations, photo, maputil };

export { StoreDataI };
