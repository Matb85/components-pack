---
title: Demo
description: See @matb85/astro-pack in action.
sidebar:
  order: 2
---

import Photo from '~/Photo.astro';
import PhotoViewer from '~/viewers/PhotoViewer.astro';
import PhotoMultiViewer from '~/viewers/PhotoMultiViewer.astro';

import "modular-slider/modular-slider.css";
import "@matb85/base-pack/dist/index.css";
import "@/index.css";

export const prefix = '/components-pack/';

export const mountains = prefix + '/thumbnail_mountains.jpg';
export const gorge = prefix + '/thumbnail_gorge.jpg';
export const bird = prefix + '/thumbnail_bird.jpg';
export const sokolica = prefix + '/thumbnail_sokolica.jpg';

## Spring group

<div class="gallery">
  {[1,2,3,4].map((i)=>(
    <Photo
      src={`${prefix}/thumbnail_spring${i}.jpg`}
      sizes={[480, 720]}
      multi
      className="photo"
      group={`spring-${i-1}`}
      alt={`Spring ${i}`} />
  ))}
</div>

## Summer group

<div class="gallery">
  {[1,2,3,4].map((i)=>(
    <Photo
      src={`${prefix}/thumbnail_summer${i}.jpg`}
      sizes={[480, 720]}
      multi
      className="photo"
      group={`summer-${i-1}`}
      alt={`Summer ${i}`} />
  ))}
</div>

## Autumn group

<div class="gallery">
  {[1,2,3,4].map((i)=>(
    <Photo
      src={`${prefix}/thumbnail_autumn${i}.jpg`}
      sizes={[480, 720, 1290]}
      multi
      className="photo"
      group={`autumn-${i-1}`}
      alt={`Autumn ${i}`} />
  ))}
</div>


## Winter group

<div class="gallery">
  {[1,2,3,4].map((i)=>(
    <Photo
      src={`${prefix}/thumbnail_winter${i}.jpg`}
      sizes={[480, 720]}
      multi
      className="photo"
      group={`winter-${i-1}`}
      alt={`Winter ${i}`} />
  ))}
</div>

<PhotoMultiViewer/>
<PhotoViewer/>